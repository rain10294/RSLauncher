appId: 'kr.rslauncher.launcher'
productName: 'RSLauncher'
artifactName: '${productName}-setup-${version}.${ext}'

copyright: 'Copyright © 2026 RSLauncher contributors'

asar: true
compression: 'maximum'

# Prevent electron-builder from inferring the upstream Helios Git remote.
# Runtime updates remain disabled while launcher.json updateUrl is empty.
publish:
  - provider: 'generic'
    url: 'https://updates.invalid/rslauncher/'
    publishAutoUpdate: false

files:
  - '!{dist,dist-client-id,release,artifacts,profile-builder,whitelist-service,auth-helper,scripts,.gitignore,.vscode,docs,dev-app-update.yml,.nvmrc,eslint.config.mjs}'

extraResources:
  - 'libraries'
  - from: 'auth-helper/publish'
    to: 'auth-helper'
    filter:
      - '**/*'

# Windows Configuration
win: 
  target: 
    - target: 'nsis'
      arch: 'x64'

# Windows Installer Configuration
nsis:
  oneClick: false
  perMachine: false
  allowElevation: true
  allowToChangeInstallationDirectory: true

# macOS Configuration
mac:
  target:
    - target: 'dmg'
      arch:
        - 'x64'
        - 'arm64'
  artifactName: '${productName}-setup-${version}-${arch}.${ext}'
  category: 'public.app-category.games'

# Linux Configuration
linux: 
  target: 'AppImage'
  maintainer: 'RSLauncher contributors'
  vendor: 'RSLauncher'
  synopsis: 'RSLauncher Minecraft Launcher'
  description: 'Helios 기반 RSLauncher. 게임 파일, Java 런타임, 서버 구성을 자동으로 관리합니다.'
  category: 'Game'


directories:
  buildResources: 'build'
  output: 'dist'
